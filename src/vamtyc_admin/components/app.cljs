(ns vamtyc-admin.components.app)

(defn app []
  [:section {:class "section"}
   [:div {:class "container"}
    [:h1 {:class "title"}
     "vamtyc-admin"]
    [:p {:class "subtitle"}
     "vamtyc rest server's admin ui"]]])
